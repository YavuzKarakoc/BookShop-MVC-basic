@model BookAndCategoryAndSeller;


<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <title></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>

<body>
    <div style="display:flex; justify-content:center;">
        <a asp-action="Create" class="btn btn-success" style="width:1000px;">Yeni Kitap Ekle</a>
        <a asp-action="CreateCategoryAndSeller" class="btn btn-primary" style="width:1000px;">Yeni Kategori ve Satıcı Ekle</a>
    </div>

    @*@if (Model.Book.Id == ViewBag.Categories.BookId) ;
        {

        }*@
    @*@foreach (var kategori in @ViewBag.Categories)
        {
            <h1 class="card-text"></h1>
        }*@

    <div class="container-fluid">
        <div class="row mt-2">
            @foreach (var kitap in Model.Books)
            {
                <div class="card col-3"  style="display:flex; justify-content:center;">
                    <img src=@kitap.ImgUrl style="height:360px;" class="card-img-top" alt="kitap fotosu">
                    <div class="card-body" >
                        <h5 class="card-title">@kitap.Name</h5>
                        <h6 class="card-text">@kitap.AuthorName</h6>
                        @*<p class="card-text">@Model.CategoryBook.Category.Name</p>*@

                        @*@foreach (var item in Model.Books)
                            {
                                @if (kitap.Book.Id == item.Id)
                                {

                                }
                            }*@

                        @*@foreach (var satici in Model.SellerBooks)
                            {
                                <p class="card-text">@satici.Seller.Name</p>

                            }*@

                        @*@foreach (var category in @ViewBag.Categories)
                            {
                                <p class="card-text">@category.Name</p>

                            }
                            @foreach (var satici in @ViewBag.Sellers)
                            {
                                <p class="card-text">@satici.Name</p>

                            }*@
                        <h3 class="card-title">@kitap.Price TL</h3>
                        <form style="display:flex; justify-content:space-around;">
                            <a  asp-action="Detail" asp-route-id="@kitap.Id" class="btn btn-primary">Detayları</a>
                            <a asp-action="Delete" asp-route-id="@kitap.Id" class="btn btn-danger">Kitabı Sil</a>
                            <a asp-action="Update" asp-route-id="@kitap.Id" class="btn btn-success">Güncelle</a>
                        </form>
                    </div>
                </div>
            }
        </div>
    </div>
    <script>
        console.log(Sellers)
    </script>
</body>
</html>